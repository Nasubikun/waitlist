{
  "name": "backend",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "migrate-dev": "npx wrangler d1 execute waitlist --local --file=./schema.sql",
    "migrate-prod": "npx wrangler d1 execute waitlist --remote --file=./schema.sql"
  },
  "dependencies": {
    "@hono/zod-openapi": "^0.19.0",
    "hono": "^4.7.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250214.0",
    "wrangler": "^3.109.2"
  }
}